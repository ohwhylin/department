@{
    ViewData["Title"] = "DisciplineBlocks";
}

<div class="list-shell">
    <div class="list-head">
        <div>
            <h2 class="list-title">Список DisciplineBlocks</h2>
            <div class="list-sub">Управление сущностью DisciplineBlock</div>
        </div>
        <div class="d-flex gap-2">
            <a class="btn btn-app-primary" asp-controller="DisciplineBlocks" asp-action="Create">Добавить</a>
        </div>
    </div>

    @if (ViewBag.DisciplineBlocksList != null)
    {
        @foreach (var item in ViewBag.DisciplineBlocksList)
        {
            <div class="entity-row">
                <div class="entity-accent"></div>
                <div class="entity-content">
                    <div class="entity-main">
                        <h5 class="entity-title">@item.Title</h5>
                        <div class="entity-meta">
                            <span class="meta-chip">Id: @item.Id</span>
                            <span class="meta-chip">@item.Title</span>
                            <span class="meta-chip">@item.DisciplineBlockBlueAsteriskName</span>
                            <span class="meta-chip">@item.DisciplineBlockUseForGrouping</span>
                            <span class="meta-chip">@item.DisciplineBlockOrder</span>
                        </div>
                    </div>
                </div>
                <div class="entity-actions">
                    <div class="actions-stack">
                        <a class="btn btn-sm btn-app-outline" asp-controller="DisciplineBlocks" asp-action="Details" asp-route-id="@item.Id">Подробнее</a>
                        <a class="btn btn-sm btn-app-outline" asp-controller="DisciplineBlocks" asp-action="Update">Изменить</a>
                        <a class="btn btn-sm btn-app-outline" asp-controller="DisciplineBlocks" asp-action="Delete">Удалить</a>
                    </div>
                </div>
            </div>
        }
    }
</div>
