@{
    ViewData["Title"] = "Lecturers";
}

<div class="list-shell">
    <div class="list-head">
        <div>
            <h2 class="list-title">Список Lecturers</h2>
            <div class="list-sub">Управление сущностью Lecturer</div>
        </div>
        <div class="d-flex gap-2">
            <a class="btn btn-app-primary" asp-controller="Lecturers" asp-action="Create">Добавить</a>
        </div>
    </div>

    @if (ViewBag.LecturersList != null)
    {
        @foreach (var item in ViewBag.LecturersList)
        {
            <div class="entity-row">
                <div class="entity-accent"></div>
                <div class="entity-content">
                    <div class="entity-main">
                        <h5 class="entity-title">@item.FirstName</h5>
                        <div class="entity-meta">
                            <span class="meta-chip">Id: @item.Id</span>
                            <span class="meta-chip">@item.LecturerStudyPost</span>
                            <span class="meta-chip">@item.LecturerDepartmentPost</span>
                            <span class="meta-chip">@item.FirstName</span>
                            <span class="meta-chip">@item.LastName</span>
                        </div>
                    </div>
                </div>
                <div class="entity-actions">
                    <div class="actions-stack">
                        <a class="btn btn-sm btn-app-outline" asp-controller="Lecturers" asp-action="Details" asp-route-id="@item.Id">Подробнее</a>
                        <a class="btn btn-sm btn-app-outline" asp-controller="Lecturers" asp-action="Update">Изменить</a>
                        <a class="btn btn-sm btn-app-outline" asp-controller="Lecturers" asp-action="Delete">Удалить</a>
                    </div>
                </div>
            </div>
        }
    }
</div>
