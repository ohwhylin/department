@{
    ViewData["Title"] = "AcademicPlanRecords";
}

<div class="list-shell">
    <div class="list-head">
        <div>
            <h2 class="list-title">Список AcademicPlanRecords</h2>
            <div class="list-sub">Управление сущностью AcademicPlanRecord</div>
        </div>
        <div class="d-flex gap-2">
            <a class="btn btn-app-primary" asp-controller="AcademicPlanRecords" asp-action="Create">Добавить</a>
        </div>
    </div>

    @if (ViewBag.AcademicPlanRecordsList != null)
    {
        @foreach (var item in ViewBag.AcademicPlanRecordsList)
        {
            <div class="entity-row">
                <div class="entity-accent"></div>
                <div class="entity-content">
                    <div class="entity-main">
                        <h5 class="entity-title">@item.Id</h5>
                        <div class="entity-meta">
                            <span class="meta-chip">Id: @item.Id</span>
                            <span class="meta-chip">@item.AcademicPlan</span>
                            <span class="meta-chip">@item.Discipline</span>
                            <span class="meta-chip">@item.AcademicPlanRecord</span>
                            <span class="meta-chip">@item.InDepartment</span>
                        </div>
                    </div>
                </div>
                <div class="entity-actions">
                    <div class="actions-stack">
                        <a class="btn btn-sm btn-app-outline" asp-controller="AcademicPlanRecords" asp-action="Details" asp-route-id="@item.Id">Подробнее</a>
                        <a class="btn btn-sm btn-app-outline" asp-controller="AcademicPlanRecords" asp-action="Update">Изменить</a>
                        <a class="btn btn-sm btn-app-outline" asp-controller="AcademicPlanRecords" asp-action="Delete">Удалить</a>
                    </div>
                </div>
            </div>
        }
    }
</div>
