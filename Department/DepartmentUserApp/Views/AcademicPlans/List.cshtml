@{
    ViewData["Title"] = "AcademicPlans";
}

<div class="list-shell">
    <div class="list-head">
        <div>
            <h2 class="list-title">Список AcademicPlans</h2>
            <div class="list-sub">Управление сущностью AcademicPlan</div>
        </div>
        <div class="d-flex gap-2">
            <a class="btn btn-app-primary" asp-controller="AcademicPlans" asp-action="Create">Добавить</a>
        </div>
    </div>

    @if (ViewBag.AcademicPlansList != null)
    {
        @foreach (var item in ViewBag.AcademicPlansList)
        {
            <div class="entity-row">
                <div class="entity-accent"></div>
                <div class="entity-content">
                    <div class="entity-main">
                        <h5 class="entity-title">@item.Id</h5>
                        <div class="entity-meta">
                            <span class="meta-chip">Id: @item.Id</span>
                            <span class="meta-chip">@item.EducationDirection</span>
                            <span class="meta-chip">@item.AcademicCourses</span>
                            <span class="meta-chip">@item.Year</span>
                        </div>
                    </div>
                </div>
                <div class="entity-actions">
                    <div class="actions-stack">
                        <a class="btn btn-sm btn-app-outline" asp-controller="AcademicPlans" asp-action="Details" asp-route-id="@item.Id">Подробнее</a>
                        <a class="btn btn-sm btn-app-outline" asp-controller="AcademicPlans" asp-action="Update">Изменить</a>
                        <a class="btn btn-sm btn-app-outline" asp-controller="AcademicPlans" asp-action="Delete">Удалить</a>
                    </div>
                </div>
            </div>
        }
    }
</div>
