@using DepartmentContracts.ViewModels

@{
    ViewData["Title"] = "Сведения о приказах";

    var studentOrders = ViewBag.StudentOrdersList as List<StudentOrderViewModel> ?? new List<StudentOrderViewModel>();
    var studentOrderBlocks = ViewBag.StudentOrderBlocksList as List<StudentOrderBlockViewModel> ?? new List<StudentOrderBlockViewModel>();
    var blockStudents = ViewBag.StudentOrderBlockStudentsList as List<StudentOrderBlockStudentViewModel> ?? new List<StudentOrderBlockStudentViewModel>();
}

<div class="list-shell">
    <div class="list-head">
        <div>
            <h2 class="list-title">Список приказов</h2>
            <div class="list-sub">
                Данные загружаются из 1С. Внутри приказа отображаются блоки, внутри блока — студенты.
            </div>
        </div>
        <div class="d-flex gap-2">
            <form asp-controller="StudentOrders" asp-action="Sync" method="post" class="d-inline">
                <button type="submit" class="btn btn-app-primary">Синхронизировать данные</button>
            </form>
        </div>
    </div>

    @if (TempData["Success"] != null)
    {
        <div class="alert alert-success m-3">
            @TempData["Success"]
        </div>
    }

    @if (TempData["Error"] != null)
    {
        <div class="alert alert-danger m-3">
            @TempData["Error"]
        </div>
    }

    @if (studentOrders.Count > 0)
    {
        @foreach (var order in studentOrders)
        {
            var orderBlocks = studentOrderBlocks.Where(x => x.StudentOrderId == order.Id).ToList();
            var collapseId = $"orderBlocks_{order.Id}";

            <div class="plan-block">
                <div class="entity-row">
                    <div class="entity-accent"></div>

                    <div class="entity-content">
                        <div class="entity-main">
                            <h5 class="entity-title">Приказ №@order.OrderNumber</h5>
                            <div class="entity-meta">
                                <span class="meta-chip">Id: @order.Id</span>
                                <span class="meta-chip">Тип: @order.StudentOrderType</span>
                                <span class="meta-chip">Блоков: @orderBlocks.Count</span>
                            </div>
                        </div>
                    </div>

                    <div class="entity-actions">
                        <div class="actions-stack">
                            <a class="btn btn-sm btn-app-outline"
                               asp-controller="StudentOrders"
                               asp-action="Details"
                               asp-route-id="@order.Id">
                                Подробнее
                            </a>

                            <button class="btn btn-sm btn-app-outline"
                                    type="button"
                                    data-bs-toggle="collapse"
                                    data-bs-target="#@collapseId"
                                    aria-expanded="false"
                                    aria-controls="@collapseId">
                                Показать блоки
                            </button>
                        </div>
                    </div>
                </div>

                <div class="collapse" id="@collapseId">
                    <div class="plan-records-wrap">
                        @if (orderBlocks.Count > 0)
                        {
                            @foreach (var block in orderBlocks)
                            {
                                var currentBlockStudents = blockStudents
                                    .Where(x => x.StudentOrderBlockId == block.Id)
                                    .ToList();

                                <div class="order-block-card">
                                    <div class="order-block-head">
                                        <div class="order-block-info">
                                            <div class="plan-record-title">
                                                Блок приказа №@block.Id
                                            </div>
                                            <div class="entity-meta">
                                                <span class="meta-chip">Тип блока: @block.StudentOrderType</span>
                                                <span class="meta-chip">Направление: @block.EducationDirection</span>
                                                <span class="meta-chip">Студентов: @currentBlockStudents.Count</span>
                                            </div>
                                        </div>

                                        <div class="plan-record-actions">
                                            <a class="btn btn-sm btn-app-outline"
                                               asp-controller="StudentOrderBlocks"
                                               asp-action="Details"
                                               asp-route-id="@block.Id">
                                                Подробнее
                                            </a>
                                        </div>
                                    </div>

                                    <div class="student-list">
                                        @if (currentBlockStudents.Count > 0)
                                        {
                                            @foreach (var blockStudent in currentBlockStudents)
                                            {
                                                <div class="student-item">
                                                    <div class="student-item__main">
                                                        <div class="student-item__title">
                                                            @blockStudent.Student
                                                        </div>

                                                        <div class="entity-meta">
                                                            <span class="meta-chip">Id записи: @blockStudent.Id</span>

                                                            @if (!string.IsNullOrWhiteSpace(blockStudent.StudentGroupFrom))
                                                            {
                                                                <span class="meta-chip">Из группы: @blockStudent.StudentGroupFrom</span>
                                                            }

                                                            @if (!string.IsNullOrWhiteSpace(blockStudent.StudentGroupTo))
                                                            {
                                                                <span class="meta-chip">В группу: @blockStudent.StudentGroupTo</span>
                                                            }
                                                        </div>
                                                    </div>

                                                    <div class="student-item__actions">
                                                        <a class="btn btn-sm btn-app-outline"
                                                           asp-controller="StudentOrderBlockStudents"
                                                           asp-action="Details"
                                                           asp-route-id="@blockStudent.Id">
                                                            Подробнее
                                                        </a>
                                                    </div>
                                                </div>
                                            }
                                        }
                                        else
                                        {
                                            <div class="text-muted">
                                                Для данного блока студенты отсутствуют.
                                            </div>
                                        }
                                    </div>
                                </div>
                            }
                        }
                        else
                        {
                            <div class="text-muted">
                                Для данного приказа блоки отсутствуют.
                            </div>
                        }
                    </div>
                </div>
            </div>
        }
    }
    else
    {
        <div class="alert alert-secondary m-3">
            Данные отсутствуют. Выполните синхронизацию.
        </div>
    }
</div>

@section Styles {
    <style>
        .plan-block {
            border-top: 1px solid var(--sky);
        }

        .plan-block:first-of-type {
            border-top: none;
        }

        .plan-block .entity-row {
            border-top: none;
        }

        .plan-records-wrap {
            margin-left: 8px;
            border-top: 1px solid var(--sky);
            background: #f8fbff;
            padding: 12px 14px 12px 22px;
        }

        .order-block-card {
            padding: 14px 0;
            border-bottom: 1px solid #e3eefb;
        }

        .order-block-card:last-child {
            border-bottom: none;
        }

        .order-block-head {
            display: flex;
            justify-content: space-between;
            align-items: flex-start;
            gap: 16px;
        }

        .order-block-info {
            flex: 1;
            min-width: 0;
        }

        .plan-record-title {
            margin: 0 0 6px 0;
            font-weight: 700;
            color: var(--galaxy);
            line-height: 1.2;
        }

        .plan-record-actions {
            flex-shrink: 0;
            padding-top: 2px;
        }

        .student-list {
            margin-top: 12px;
            padding-left: 12px;
            border-left: 3px solid var(--sky);
            display: flex;
            flex-direction: column;
            gap: 10px;
        }

        .student-item {
            display: flex;
            justify-content: space-between;
            align-items: flex-start;
            gap: 16px;
            background: #fff;
            border: 1px solid #e3eefb;
            border-radius: 10px;
            padding: 10px 12px;
        }

        .student-item__main {
            flex: 1;
            min-width: 0;
        }

        .student-item__title {
            font-weight: 700;
            color: var(--galaxy);
            margin-bottom: 6px;
        }

        .student-item__actions {
            flex-shrink: 0;
        }
    </style>
}

@section Scripts {
    <script>
        document.addEventListener('DOMContentLoaded', function () {
            const collapseElements = document.querySelectorAll('.collapse');

            collapseElements.forEach(function (collapseElement) {
                collapseElement.addEventListener('shown.bs.collapse', function () {
                    const button = document.querySelector('[data-bs-target="#' + collapseElement.id + '"]');
                    if (button) {
                        button.textContent = 'Скрыть блоки';
                    }
                });

                collapseElement.addEventListener('hidden.bs.collapse', function () {
                    const button = document.querySelector('[data-bs-target="#' + collapseElement.id + '"]');
                    if (button) {
                        button.textContent = 'Показать блоки';
                    }
                });
            });
        });
    </script>
}