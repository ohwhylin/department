@model DepartmentContracts.BindingModels.StudentOrderBindingModel
@using DepartmentDataModels.Enums
@using Newtonsoft.Json;
@{
    ViewData["Title"] = "StudentOrderUpdate";
}
<header>
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
</header>
<style>
    .validation-summary-errors { color: #dc3545; margin-bottom: 15px; }
    .field-validation-error { color: #dc3545; font-size: 0.875em; }
    .input-validation-error { border-color: #dc3545; }
    .alert-danger { color: #721c24; background-color: #f8d7da; border-color: #f5c6cb; padding: .75rem 1.25rem; margin-bottom: 1rem; border: 1px solid transparent; border-radius: .25rem; }
</style>

<div class="text-center">
    <h2 class="display-4">Данные StudentOrder</h2>
</div>

@if (TempData["Error"] != null)
{
    <div class="alert alert-danger">
        @TempData["Error"]
    </div>
}

<form method="post">
    <div class="row">
        <div class="col-4">StudentOrder:</div>
        <div class="col-8">
            <select asp-for="Id" class="form-control" asp-items="@(new SelectList(@ViewBag.StudentOrdersList, "Id", "Id"))"></select>
        </div>
    </div>
    <div class="row">
        <div class="col-4">Order Number:</div>
        <div class="col-8">
            <input asp-for="OrderNumber" class="form-control" />
            <span asp-validation-for="OrderNumber" class="text-danger"></span>
        </div>
    </div>
    <div class="row">
        <div class="col-4">Student Order Type:</div>
        <div class="col-8">
            <select asp-for="StudentOrderType" class="form-control" asp-items="Html.GetEnumSelectList<StudentOrderType>()"></select>
            <span asp-validation-for="StudentOrderType" class="text-danger"></span>
        </div>
    </div>
    <div class="row">
        <div class="col-8"></div>
        <div class="col-4"><input type="submit" value="Сохранить" class="btn btn-primary" /></div>
    </div>
</form>

<script>
    $(document).ready(function () {
        $('#Id').change(function () {
            var selectedId = $(this).val();
            var entities = @Html.Raw(JsonConvert.SerializeObject(ViewBag.StudentOrdersList));
            var selected = entities.find(d => d.Id == selectedId);
            if (selected) {
                $('#OrderNumber').val(selected.OrderNumber);
                $('#StudentOrderType').val(selected.StudentOrderType);
            }
        });
        $('#Id').trigger('change');
    });
</script>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
