@{
    ViewData["Title"] = "Disciplines";
}

<div class="list-shell">
    <div class="list-head">
        <div>
            <h2 class="list-title">Список Disciplines</h2>
            <div class="list-sub">Управление сущностью Discipline</div>
        </div>
        <div class="d-flex gap-2">
            <a class="btn btn-app-primary" asp-controller="Disciplines" asp-action="Create">Добавить</a>
        </div>
    </div>

    @if (ViewBag.DisciplinesList != null)
    {
        @foreach (var item in ViewBag.DisciplinesList)
        {
            <div class="entity-row">
                <div class="entity-accent"></div>
                <div class="entity-content">
                    <div class="entity-main">
                        <h5 class="entity-title">@item.DisciplineName</h5>
                        <div class="entity-meta">
                            <span class="meta-chip">Id: @item.Id</span>
                            <span class="meta-chip">@item.DisciplineBlock</span>
                            <span class="meta-chip">@item.DisciplineName</span>
                            <span class="meta-chip">@item.DisciplineShortName</span>
                            <span class="meta-chip">@item.DisciplineDescription</span>
                        </div>
                    </div>
                </div>
                <div class="entity-actions">
                    <div class="actions-stack">
                        <a class="btn btn-sm btn-app-outline" asp-controller="Disciplines" asp-action="Details" asp-route-id="@item.Id">Подробнее</a>
                        <a class="btn btn-sm btn-app-outline" asp-controller="Disciplines" asp-action="Update">Изменить</a>
                        <a class="btn btn-sm btn-app-outline" asp-controller="Disciplines" asp-action="Delete">Удалить</a>
                    </div>
                </div>
            </div>
        }
    }
</div>
