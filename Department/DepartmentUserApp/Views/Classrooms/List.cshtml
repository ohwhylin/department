@{
    ViewData["Title"] = "Classrooms";
}

<div class="list-shell">
    <div class="list-head">
        <div>
            <h2 class="list-title">Список Classrooms</h2>
            <div class="list-sub">Управление сущностью Classroom</div>
        </div>
        <div class="d-flex gap-2">
            <a class="btn btn-app-primary" asp-controller="Classrooms" asp-action="Create">Добавить</a>
        </div>
    </div>

    @if (ViewBag.ClassroomsList != null)
    {
        @foreach (var item in ViewBag.ClassroomsList)
        {
            <div class="entity-row">
                <div class="entity-accent"></div>
                <div class="entity-content">
                    <div class="entity-main">
                        <h5 class="entity-title">@item.Id</h5>
                        <div class="entity-meta">
                            <span class="meta-chip">Id: @item.Id</span>
                            <span class="meta-chip">@item.Number</span>
                            <span class="meta-chip">@item.ClassroomType</span>
                            <span class="meta-chip">@item.Capacity</span>
                            <span class="meta-chip">@item.NotUseInSchedule</span>
                        </div>
                    </div>
                </div>
                <div class="entity-actions">
                    <div class="actions-stack">
                        <a class="btn btn-sm btn-app-outline" asp-controller="Classrooms" asp-action="Details" asp-route-id="@item.Id">Подробнее</a>
                        <a class="btn btn-sm btn-app-outline" asp-controller="Classrooms" asp-action="Update">Изменить</a>
                        <a class="btn btn-sm btn-app-outline" asp-controller="Classrooms" asp-action="Delete">Удалить</a>
                    </div>
                </div>
            </div>
        }
    }
</div>
