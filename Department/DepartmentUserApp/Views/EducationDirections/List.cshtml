@{
    ViewData["Title"] = "EducationDirections";
}

<div class="list-shell">
    <div class="list-head">
        <div>
            <h2 class="list-title">Список EducationDirections</h2>
            <div class="list-sub">Управление сущностью EducationDirection</div>
        </div>
        <div class="d-flex gap-2">
            <a class="btn btn-app-primary" asp-controller="EducationDirections" asp-action="Create">Добавить</a>
        </div>
    </div>

    @if (ViewBag.EducationDirectionsList != null)
    {
        @foreach (var item in ViewBag.EducationDirectionsList)
        {
            <div class="entity-row">
                <div class="entity-accent"></div>
                <div class="entity-content">
                    <div class="entity-main">
                        <h5 class="entity-title">@item.Title</h5>
                        <div class="entity-meta">
                            <span class="meta-chip">Id: @item.Id</span>
                            <span class="meta-chip">@item.Cipher</span>
                            <span class="meta-chip">@item.ShortName</span>
                            <span class="meta-chip">@item.Title</span>
                            <span class="meta-chip">@item.Qualification</span>
                        </div>
                    </div>
                </div>
                <div class="entity-actions">
                    <div class="actions-stack">
                        <a class="btn btn-sm btn-app-outline" asp-controller="EducationDirections" asp-action="Details" asp-route-id="@item.Id">Подробнее</a>
                        <a class="btn btn-sm btn-app-outline" asp-controller="EducationDirections" asp-action="Update">Изменить</a>
                        <a class="btn btn-sm btn-app-outline" asp-controller="EducationDirections" asp-action="Delete">Удалить</a>
                    </div>
                </div>
            </div>
        }
    }
</div>
