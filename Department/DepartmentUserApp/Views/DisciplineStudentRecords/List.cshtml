@{
    ViewData["Title"] = "DisciplineStudentRecords";
}

<div class="list-shell">
    <div class="list-head">
        <div>
            <h2 class="list-title">Список DisciplineStudentRecords</h2>
            <div class="list-sub">Управление сущностью DisciplineStudentRecord</div>
        </div>
        <div class="d-flex gap-2">
            <a class="btn btn-app-primary" asp-controller="DisciplineStudentRecords" asp-action="Create">Добавить</a>
        </div>
    </div>

    @if (ViewBag.DisciplineStudentRecordsList != null)
    {
        @foreach (var item in ViewBag.DisciplineStudentRecordsList)
        {
            <div class="entity-row">
                <div class="entity-accent"></div>
                <div class="entity-content">
                    <div class="entity-main">
                        <h5 class="entity-title">@item.Id</h5>
                        <div class="entity-meta">
                            <span class="meta-chip">Id: @item.Id</span>
                            <span class="meta-chip">@item.Discipline</span>
                            <span class="meta-chip">@item.Student</span>
                            <span class="meta-chip">@item.Semester</span>
                            <span class="meta-chip">@item.Variant</span>
                        </div>
                    </div>
                </div>
                <div class="entity-actions">
                    <div class="actions-stack">
                        <a class="btn btn-sm btn-app-outline" asp-controller="DisciplineStudentRecords" asp-action="Details" asp-route-id="@item.Id">Подробнее</a>
                        <a class="btn btn-sm btn-app-outline" asp-controller="DisciplineStudentRecords" asp-action="Update">Изменить</a>
                        <a class="btn btn-sm btn-app-outline" asp-controller="DisciplineStudentRecords" asp-action="Delete">Удалить</a>
                    </div>
                </div>
            </div>
        }
    }
</div>
